apiVersion: apps/v1
kind: Deployment
metadata:
  name: post-react-sub
spec:
  selector:
    matchLabels:
      app: post-react-sub
  template:
    metadata:
      labels:
        app: post-react-sub
    spec:
      containers:
      - name: post-react-sub
        image: gcr.io/level-slate-373806/dev-echo-connect-post-react-sub
        env:
        - name: MONGO_URI
          valueFrom:
            secretKeyRef:
              name: secret-envs
              key: MONGO_URI
        - name: GOOGLE_APPLICATION_CREDENTIALS_CONTENT
          valueFrom:
            secretKeyRef:
              name: secret-envs
              key: GOOGLE_APPLICATION_CREDENTIALS_CONTENT
